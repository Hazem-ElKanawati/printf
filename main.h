#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>


int _printf(const char *format, ...);
void _puts(char *str);
int _strlen(const char *s);
int _putchar(char c);

#endif
